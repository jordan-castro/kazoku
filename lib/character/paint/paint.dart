/// This is a object which goes on top of a Character.
class Paint {
  final String texturePath;
  final Map<String, String?> colorToColor;

  Paint({required this.texturePath, required this.colorToColor});
}
